# For local development, leave this as /api and use Vite's proxy to Flask on port 5000.
VITE_API_BASE_URL=/api

# For GitHub Pages deployment, set this to your hosted backend URL, for example:
# VITE_API_BASE_URL=https://your-backend.example.com/api
